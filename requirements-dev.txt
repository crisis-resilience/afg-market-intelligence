#
# GENERATED FILE — do not edit by hand.
#
# Compiled from requirements-dev.in. Regenerate with:
#     pip-compile --strip-extras requirements-dev.in -o requirements-dev.txt
#
# Install alongside the runtime lock for local development and CI:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Note: anyio, certifi, h11, idna and typing-extensions also appear in
# requirements.txt. That is expected, not drift — requirements-dev.in's
# `-c requirements.txt` is a CONSTRAINT, which pins the version a transitive
# dependency resolves to but does not remove it from the output. httpx pulls
# all five, so they are listed here too, at exactly the versions the runtime
# lock already fixes.
#
anyio==4.14.1
certifi==2025.8.3
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.10
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pygments==2.20.0
pytest==9.1.1
ruff==0.15.21
typing-extensions==4.15.0
